name: ranch_components
description: Package with the game components for the Very Good Ranch game.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flame: ^1.1.1
  flutter:
    sdk: flutter

dev_dependencies:
  flame_test: ^1.4.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^2.4.0

flutter:
  assets:
    - assets/images/